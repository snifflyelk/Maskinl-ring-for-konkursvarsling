# Data til vedlegg

Denne mappen er holdt tom for a unnga duplisering av store filer.

For eksakt reproduksjon brukes snapshot-filen i prosjektroten:

- `alle_enheter.json`

Se `../metadata_datafreeze.json` for hash, storrelse og tidsstempel.
